# Model Artifacts

Place exported ONNX model files here for local development.

Default expected path:

- `model/artifacts/model.onnx` for local runs
- `/app/model/model.onnx` inside the backend container
- or override with `MEDIMG_MODEL_PATH`
